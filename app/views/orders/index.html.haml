%h1 Orders
%table
  %thead
    %tr
      %th Oder
      %th Sales Taxes
      %th Total
      %th{:colspan => "3"}
  %tbody
    - @orders.each do |oder|
      %tr
        %td= oder.id
        %td= oder.sales_taxes
        %td= oder.total
        %td= button_to 'Show', oder, method: :get
        %td= button_to 'Delete', oder, method: :delete
%br
= link_to 'New oder', new_order_path